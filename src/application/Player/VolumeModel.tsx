import React from "react"
import "./style/VolumeModel.less"

const prefix = "VolumeModel"
const VolumeModel: React.FC = () => {
  return <div className={`${prefix}-backgroud`}></div>
}
export default VolumeModel
